import { SkeletonColor } from './Skeleton.types';

export const SKELETON_COLOR: Record<SkeletonColor, string> = {
  primary: '--bg-overlay-primary',
  secondary: '--bg-overlay-secondary',
  tertiary: '--bg-overlay-tertiary'
};
